# WireGuard — single-hop example
#
# Placeholder values use the documentation ranges from RFC 5737 (IPv4) and
# RFC 3849 (IPv6) so they can't conflict with anything real. Replace each
# __PLACEHOLDER__ with the value from your provider's generated config.

[Interface]
PrivateKey = __YOUR_PRIVATE_KEY_BASE64__
Address = 198.51.100.2/32, 2001:db8:1::2/128
DNS = 198.51.100.1, 2001:db8:1::1

[Peer]
PublicKey = __SERVER_PUBLIC_KEY_BASE64__
AllowedIPs = 0.0.0.0/0, ::/0
Endpoint = wg-single.vpn.example.com:51820
